package in.kabadiwala.connect.model; import org.springframework.data.annotation.*; import org.springframework.data.mongodb.core.index.Indexed; import org.springframework.data.mongodb.core.mapping.Document; import java.time.Instant; import java.util.*; @Document("material_categories") public class MaterialCategory {@Id public String id; @Indexed(unique=true) public String code; public Map<String,String> names=new HashMap<>(); public String iconUrl; public List<String> units=new ArrayList<>(); public boolean active=true; @CreatedDate public Instant createdAt; @LastModifiedDate public Instant updatedAt;}