import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{Button,Status}from'../components/UI';describe('UI primitives',()=>{it('renders accessible action',()=>{render(<Button>Confirm</Button>);expect(screen.getByRole('button',{name:'Confirm'})).toBeTruthy()});it('renders text with status color',()=>{render(<Status value="PICKUP_SCHEDULED"/>);expect(screen.getByText(/PICKUP SCHEDULED/)).toBeTruthy()})});
